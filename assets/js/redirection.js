// get queue data
// update contents with audio data
// update queue data
// redirect to the location
{
	try {
		// const audio = document.querySelector("audio");
		// console.log(audio.currentTime);
	} catch (error) {
		console.log(error);
	}
}
